#!/usr/bin/python3
def multiple_returns(sentence):
    length = len(sentence)
    if len == 0:
        first_let = None
    else:
        first_let = sentence[0]
    new_tuple = (length, first_let)
    return new_tuple
